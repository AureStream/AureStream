[package]
name = "aurestream-config"
version = "0.1.0"
edition = "2021"
description = "Decode proxy subscription bodies into engine-agnostic ProxyNode values"

[dependencies]
base64 = "0.22"
serde_json = "1"
urlencoding = "2"
